LANG=ja_JP.UTF-8
PS1='[\u@\h] \w\$ '
export LESS='--no-init'

set -o ignoreeof

# command history configuration
shopt -s histappend
HISTSIZW=100000
HISTFILESIZE=100000

# alias configuration
alias ls='ls -F'
alias la='ls -a'
alias ll='ls l'
alias cp='cp -i'
alias rm='rm -i'
alias mv='mv -i'
alias mkdir ='mkdir -p'
