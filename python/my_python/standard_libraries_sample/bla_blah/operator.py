# equality operator
    # <>  not equal 
    # !=  not equal
# caculator
    # **  uppower (ex: 2**3 == 8)
    # @ matrix multiplication( must import a module)
    # // floor division (ex: 3 // 2 == 1)
# binary 
    # & binary and ( and caculate )
    # | binary or ( 1 | 0 == 1, 1|1 == 1) 
    # ^ binary xor (0 ^ 0 == 0, 1^0 == 1, 1^1 == 0 )
    # ~ binary ones complement
    # << , >> binary shift
# is evaluate to true when either side point the same object or value ( x is y == True -> x == y )
# is not evaluate to false when either side point the same object
