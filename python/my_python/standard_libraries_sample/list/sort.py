def sort_the_list(list):
    # This will change the whole list
    list.sort()
    list.sort(Reverse = True)
    # This will return a diff list
    # Function name can be len()
    sorted(list, function_name, Reverse = True)
